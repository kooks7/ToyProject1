<%- include('./includes/head.ejs') %>
<link rel="stylesheet" href="/css/nav.css">
  <body>
<%- include('./includes/nav.ejs') %>
    <h1>Be A Artista</h1>

    <div>
      <% for (let cont of content) { %>
      <img src="<%= cont.imageUrl %>" alt="<%= cont.title %>" />
      <h2><%= cont.title %></h2>
      <p><%= cont.description %></p>
      <h5><%= cont.time1 %>전 포스팅</h5>
      <% } %>
    </div>
  </body>
</html>
